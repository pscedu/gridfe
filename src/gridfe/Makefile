# $Id$

SYSROOT = ../..

include ${SYSROOT}/lib/mk/defs.mk

CLASSES += Page.class GridFE.class
CLEAN += DelegationHandler.class NavigationMenu.class
SUBDIRS += gridint www
TARGET = ${CLASSES}

include ${MAINMK}
